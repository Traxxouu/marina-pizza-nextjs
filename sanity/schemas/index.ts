 
import product from './product';
import category from './category';
import notification from './notification';
import settings from './settings';

export const schemaTypes = [product, category, notification, settings];